import __buffer_polyfill from 'vite-plugin-node-polyfills/shims/buffer'
import __global_polyfill from 'vite-plugin-node-polyfills/shims/global'
import __process_polyfill from 'vite-plugin-node-polyfills/shims/process'

globalThis.Buffer = globalThis.Buffer || __buffer_polyfill
globalThis.global = globalThis.global || __global_polyfill
globalThis.process = globalThis.process || __process_polyfill

import {
  require_bs58
} from "./chunk-5X45PFY6.js";
import {
  Block,
  Psbt,
  Transaction,
  address_exports,
  require_dist as require_dist4
} from "./chunk-2HPYGSLD.js";
import {
  init_sha256,
  sha256
} from "./chunk-5RQDRUHP.js";
import {
  __toESM,
  require_dist,
  require_dist2,
  require_dist3
} from "./chunk-7D4CR34R.js";

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/index.js
var import_dist223 = __toESM(require_dist());
var import_dist224 = __toESM(require_dist2());
var import_dist225 = __toESM(require_dist3());

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/actions/getBalance.js
var import_dist = __toESM(require_dist(), 1);
var import_dist2 = __toESM(require_dist2(), 1);
var import_dist3 = __toESM(require_dist3(), 1);
async function getBalance(client, params) {
  const data = await client.request({
    method: "getBalance",
    params
  }, { dedupe: true });
  return data;
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/actions/getBlock.js
var import_dist13 = __toESM(require_dist(), 1);
var import_dist14 = __toESM(require_dist2(), 1);
var import_dist15 = __toESM(require_dist3(), 1);

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/errors/block.js
var import_dist10 = __toESM(require_dist(), 1);
var import_dist11 = __toESM(require_dist2(), 1);
var import_dist12 = __toESM(require_dist3(), 1);

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/errors/base.js
var import_dist7 = __toESM(require_dist(), 1);
var import_dist8 = __toESM(require_dist2(), 1);
var import_dist9 = __toESM(require_dist3(), 1);

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/version.js
var import_dist4 = __toESM(require_dist(), 1);
var import_dist5 = __toESM(require_dist2(), 1);
var import_dist6 = __toESM(require_dist3(), 1);
var version = "0.3.1";

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/errors/base.js
var errorConfig = {
  version: `bigmi@${version}`
};
var BaseError = class _BaseError extends Error {
  constructor(shortMessage, args = {}) {
    const details = (() => {
      var _a;
      if (args.cause instanceof _BaseError) {
        return args.cause.details;
      }
      if ((_a = args.cause) == null ? void 0 : _a.message) {
        return args.cause.message;
      }
      return args.details;
    })();
    const message = [
      shortMessage || "An error occurred.",
      "",
      ...args.metaMessages ? [...args.metaMessages, ""] : [],
      ...details ? [`Details: ${details}`] : [],
      ...errorConfig.version ? [`Version: ${errorConfig.version}`] : []
    ].join("\n");
    super(message, args.cause ? { cause: args.cause } : void 0);
    this.name = "BaseError";
    this.details = details;
    this.metaMessages = args.metaMessages;
    this.name = args.name ?? this.name;
    this.shortMessage = shortMessage;
    this.version = version;
  }
  walk(fn) {
    return walk(this, fn);
  }
};
function walk(err, fn) {
  if (fn == null ? void 0 : fn(err)) {
    return err;
  }
  if (err && typeof err === "object" && "cause" in err && err.cause !== void 0) {
    return walk(err.cause, fn);
  }
  return fn ? null : err;
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/errors/block.js
var BlockNotFoundError = class extends BaseError {
  constructor({ blockHash, blockNumber }) {
    let identifier = "Block";
    if (blockHash) {
      identifier = `Block at hash "${blockHash}"`;
    }
    if (blockNumber) {
      identifier = `Block at number "${blockNumber}"`;
    }
    super(`${identifier} could not be found.`, { name: "BlockNotFoundError" });
  }
};

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/actions/getBlock.js
async function getBlock(client, { blockHash, blockNumber }) {
  let blockHex;
  try {
    let _blockHash = blockHash;
    if (!_blockHash && blockNumber) {
      _blockHash = await client.request({
        method: "getblockhash",
        params: [blockNumber]
      }, { dedupe: true });
    }
    if (_blockHash) {
      blockHex = await client.request({
        method: "getblock",
        params: [_blockHash, 0]
      }, { dedupe: true });
    }
  } catch (_error) {
    throw new BlockNotFoundError({ blockHash, blockNumber });
  }
  if (!blockHex) {
    throw new BlockNotFoundError({ blockHash, blockNumber });
  }
  return Block.fromHex(blockHex);
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/actions/getBlockCount.js
var import_dist16 = __toESM(require_dist(), 1);
var import_dist17 = __toESM(require_dist2(), 1);
var import_dist18 = __toESM(require_dist3(), 1);
async function getBlockCount(client) {
  const data = await client.request({
    method: "getblockcount",
    params: []
  }, { dedupe: true });
  return data;
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/actions/getBlockStats.js
var import_dist19 = __toESM(require_dist(), 1);
var import_dist20 = __toESM(require_dist2(), 1);
var import_dist21 = __toESM(require_dist3(), 1);
async function getBlockStats(client, { blockHash, blockNumber, stats }) {
  const blockHashOrNumber = blockHash || blockNumber;
  if (!blockHashOrNumber) {
    throw new BlockNotFoundError({ blockHash, blockNumber });
  }
  try {
    const params = [
      blockHashOrNumber
    ];
    if (stats) {
      params.push(stats);
    }
    const data = await client.request({
      method: "getblockstats",
      params
    }, { dedupe: true });
    return data;
  } catch (_error) {
    throw new BlockNotFoundError({ blockHash, blockNumber });
  }
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/actions/getUTXOTransaction.js
var import_dist25 = __toESM(require_dist(), 1);
var import_dist26 = __toESM(require_dist2(), 1);
var import_dist27 = __toESM(require_dist3(), 1);

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/errors/transaction.js
var import_dist22 = __toESM(require_dist(), 1);
var import_dist23 = __toESM(require_dist2(), 1);
var import_dist24 = __toESM(require_dist3(), 1);
var TransactionNotFoundError = class extends BaseError {
  constructor({ blockHash, blockNumber, blockTag, hash, index: index2 }) {
    let identifier = "Transaction";
    if (blockTag && index2 !== void 0) {
      identifier = `Transaction at block time "${blockTag}" at index "${index2}"`;
    }
    if (blockHash && index2 !== void 0) {
      identifier = `Transaction at block hash "${blockHash}" at index "${index2}"`;
    }
    if (blockNumber && index2 !== void 0) {
      identifier = `Transaction at block number "${blockNumber}" at index "${index2}"`;
    }
    if (hash) {
      identifier = `Transaction with hash "${hash}"`;
    }
    super(`${identifier} could not be found.`, {
      name: "TransactionNotFoundError"
    });
  }
};
var TransactionReceiptNotFoundError = class extends BaseError {
  constructor({ hash }) {
    super(`Transaction receipt with hash "${hash}" could not be found. The Transaction may not be processed on a block yet.`, {
      name: "TransactionReceiptNotFoundError"
    });
  }
};
var WaitForTransactionReceiptTimeoutError = class extends BaseError {
  constructor({ hash }) {
    super(`Timed out while waiting for transaction with hash "${hash}" to be confirmed.`, { name: "WaitForTransactionReceiptTimeoutError" });
  }
};

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/actions/getUTXOTransaction.js
async function getUTXOTransaction(client, { txId, blockHash }) {
  try {
    const params = [txId, true];
    if (blockHash) {
      params.push(blockHash);
    }
    const data = await client.request({
      method: "getrawtransaction",
      params
    });
    return data;
  } catch (_error) {
    throw new TransactionNotFoundError({
      blockHash,
      hash: txId
    });
  }
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/actions/sendUTXOTransaction.js
var import_dist28 = __toESM(require_dist(), 1);
var import_dist29 = __toESM(require_dist2(), 1);
var import_dist30 = __toESM(require_dist3(), 1);
async function sendUTXOTransaction(client, { hex, maxFeeRate }) {
  const params = [hex];
  if (maxFeeRate) {
    params.push(maxFeeRate);
  }
  const data = await client.request({
    method: "sendrawtransaction",
    params
  });
  return data;
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/actions/signPsbt.js
var import_dist31 = __toESM(require_dist(), 1);
var import_dist32 = __toESM(require_dist2(), 1);
var import_dist33 = __toESM(require_dist3(), 1);
async function signPsbt(client, params) {
  const data = await client.request({
    method: "signPsbt",
    params
  }, { dedupe: true });
  return data;
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/actions/waitForTransaction.js
var import_dist58 = __toESM(require_dist(), 1);
var import_dist59 = __toESM(require_dist2(), 1);
var import_dist60 = __toESM(require_dist3(), 1);

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/utils/getAction.js
var import_dist34 = __toESM(require_dist(), 1);
var import_dist35 = __toESM(require_dist2(), 1);
var import_dist36 = __toESM(require_dist3(), 1);
function getAction(client, actionFn, name) {
  const action_implicit = client[actionFn.name];
  if (typeof action_implicit === "function") {
    return action_implicit;
  }
  const action_explicit = client[name];
  if (typeof action_explicit === "function") {
    return action_explicit;
  }
  return (params) => actionFn(client, params);
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/utils/observe.js
var import_dist37 = __toESM(require_dist(), 1);
var import_dist38 = __toESM(require_dist2(), 1);
var import_dist39 = __toESM(require_dist3(), 1);
var listenersCache = /* @__PURE__ */ new Map();
var cleanupCache = /* @__PURE__ */ new Map();
var callbackCount = 0;
function observe(observerId, callbacks, fn) {
  const callbackId = ++callbackCount;
  const getListeners = () => listenersCache.get(observerId) || [];
  const unsubscribe = () => {
    const listeners2 = getListeners();
    listenersCache.set(observerId, listeners2.filter((cb) => cb.id !== callbackId));
  };
  const unwatch = () => {
    const cleanup2 = cleanupCache.get(observerId);
    if (getListeners().length === 1 && cleanup2) {
      cleanup2();
    }
    unsubscribe();
  };
  const listeners = getListeners();
  listenersCache.set(observerId, [
    ...listeners,
    { id: callbackId, fns: callbacks }
  ]);
  if (listeners && listeners.length > 0) {
    return unwatch;
  }
  const emit = {};
  for (const key in callbacks) {
    emit[key] = (...args) => {
      var _a, _b;
      const listeners2 = getListeners();
      if (listeners2.length === 0) {
        return;
      }
      for (const listener of listeners2) {
        (_b = (_a = listener.fns)[key]) == null ? void 0 : _b.call(_a, ...args);
      }
    };
  }
  const cleanup = fn(emit);
  if (typeof cleanup === "function") {
    cleanupCache.set(observerId, cleanup);
  }
  return unwatch;
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/utils/stringify.js
var import_dist40 = __toESM(require_dist(), 1);
var import_dist41 = __toESM(require_dist2(), 1);
var import_dist42 = __toESM(require_dist3(), 1);
var stringify = (value, replacer, space) => JSON.stringify(value, (key, value_) => {
  const value2 = typeof value_ === "bigint" ? value_.toString() : value_;
  return typeof replacer === "function" ? replacer(key, value2) : value2;
}, space);

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/utils/withRetry.js
var import_dist46 = __toESM(require_dist(), 1);
var import_dist47 = __toESM(require_dist2(), 1);
var import_dist48 = __toESM(require_dist3(), 1);

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/utils/wait.js
var import_dist43 = __toESM(require_dist(), 1);
var import_dist44 = __toESM(require_dist2(), 1);
var import_dist45 = __toESM(require_dist3(), 1);
async function wait(time) {
  return new Promise((res) => setTimeout(res, time));
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/utils/withRetry.js
function withRetry(fn, { delay: delay_ = 100, retryCount = 2, shouldRetry: shouldRetry2 = () => true } = {}) {
  return new Promise((resolve, reject) => {
    const attemptRetry = async ({ count = 0 } = {}) => {
      const retry = async ({ error }) => {
        const delay = typeof delay_ === "function" ? delay_({ count, error }) : delay_;
        if (delay) {
          await wait(delay);
        }
        attemptRetry({ count: count + 1 });
      };
      try {
        const data = await fn();
        resolve(data);
      } catch (err) {
        if (count < retryCount && await shouldRetry2({ count, error: err })) {
          return retry({ error: err });
        }
        reject(err);
      }
    };
    attemptRetry();
  });
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/actions/watchBlockNumber.js
var import_dist55 = __toESM(require_dist(), 1);
var import_dist56 = __toESM(require_dist2(), 1);
var import_dist57 = __toESM(require_dist3(), 1);

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/utils/poll.js
var import_dist52 = __toESM(require_dist(), 1);
var import_dist53 = __toESM(require_dist2(), 1);
var import_dist54 = __toESM(require_dist3(), 1);

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/utils/sleep.js
var import_dist49 = __toESM(require_dist(), 1);
var import_dist50 = __toESM(require_dist2(), 1);
var import_dist51 = __toESM(require_dist3(), 1);
function sleep(ms) {
  return new Promise((resolve) => {
    setTimeout(() => resolve(null), ms);
  });
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/utils/poll.js
function poll(fn, { emitOnBegin, initialWaitTime, interval }) {
  let active = true;
  const unwatch = () => {
    active = false;
  };
  const watch = async () => {
    let data = void 0;
    if (emitOnBegin) {
      data = await fn({ unpoll: unwatch });
    }
    const initialWait = await (initialWaitTime == null ? void 0 : initialWaitTime(data)) ?? interval;
    await sleep(initialWait);
    const poll2 = async () => {
      if (!active) {
        return;
      }
      await fn({ unpoll: unwatch });
      await sleep(interval);
      poll2();
    };
    poll2();
  };
  watch();
  return unwatch;
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/actions/watchBlockNumber.js
function watchBlockNumber(client, { emitOnBegin = false, emitMissed = false, onBlockNumber, onError, pollingInterval = client.pollingInterval }) {
  let prevBlockNumber;
  const observerId = stringify([
    "watchBlockNumber",
    client.uid,
    emitOnBegin,
    emitMissed,
    pollingInterval
  ]);
  return observe(observerId, { onBlockNumber, onError }, (emit) => poll(async () => {
    var _a;
    try {
      const blockNumber = await getAction(client, getBlockCount, "getBlockCount")({ cacheTime: 0 });
      if (prevBlockNumber) {
        if (blockNumber === prevBlockNumber) {
          return;
        }
        if (blockNumber - prevBlockNumber > 1 && emitMissed) {
          for (let i = prevBlockNumber + 1; i < blockNumber; i++) {
            await emit.onBlockNumber(i, prevBlockNumber);
            prevBlockNumber = i;
          }
        }
      }
      if (!prevBlockNumber || blockNumber > prevBlockNumber) {
        await emit.onBlockNumber(blockNumber, prevBlockNumber);
        prevBlockNumber = blockNumber;
      }
    } catch (err) {
      (_a = emit.onError) == null ? void 0 : _a.call(emit, err);
    }
  }, {
    emitOnBegin,
    interval: pollingInterval
  }));
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/actions/waitForTransaction.js
async function waitForTransaction(client, { confirmations = 1, txId, txHex, senderAddress, onReplaced, pollingInterval = client.pollingInterval, retryCount = 10, retryDelay = 3e3, timeout }) {
  const observerId = stringify(["waitForTransaction", client.uid, txId]);
  let count = 0;
  let transaction;
  let replacedTransaction;
  let retrying = false;
  return new Promise((resolve, reject) => {
    if (timeout) {
      setTimeout(() => reject(new WaitForTransactionReceiptTimeoutError({ hash: txId })), timeout);
    }
    const _unobserve = observe(observerId, { onReplaced, resolve, reject }, (emit) => {
      const _unwatch = getAction(client, watchBlockNumber, "watchBlockNumber")({
        emitMissed: true,
        emitOnBegin: true,
        pollingInterval,
        async onBlockNumber(blockNumber_) {
          const done = (fn) => {
            _unwatch();
            fn();
            _unobserve();
          };
          let blockNumber = blockNumber_;
          if (retrying) {
            return;
          }
          if (count > retryCount) {
            done(() => emit.reject(new WaitForTransactionReceiptTimeoutError({
              hash: txId
            })));
          }
          try {
            if (transaction == null ? void 0 : transaction.blockhash) {
              const blockStats = await getAction(client, getBlockStats, "getBlockStats")({
                blockHash: transaction.blockhash,
                stats: ["height"]
              });
              if (confirmations > 1 && (!blockStats.height || blockNumber - blockStats.height + 1 < confirmations)) {
                return;
              }
              done(() => emit.resolve(transaction));
              return;
            }
            retrying = true;
            transaction = await withRetry(() => getAction(
              client,
              getUTXOTransaction,
              "getUTXOTransaction"
              // If transaction exists it might be the replaced one with different txId
            )({ txId: (transaction == null ? void 0 : transaction.txid) || txId }), {
              delay: retryDelay,
              retryCount
            });
            if (transaction.blockhash) {
              const blockStats = await getAction(client, getBlockStats, "getBlockStats")({
                blockHash: transaction.blockhash,
                stats: ["height"]
              });
              if (blockStats.height) {
                blockNumber = blockStats.height;
              }
            }
            retrying = false;
            if (!(transaction == null ? void 0 : transaction.confirmations)) {
              throw new TransactionReceiptNotFoundError({
                hash: txId
              });
            }
            if (transaction.confirmations < confirmations) {
              return;
            }
            done(() => emit.resolve(transaction));
          } catch (err) {
            if (err instanceof TransactionNotFoundError || err instanceof TransactionReceiptNotFoundError) {
              try {
                let getOutputAddresses = function(tx) {
                  const addresses = [];
                  for (const output of tx.outs) {
                    try {
                      const outputAddress = address_exports.fromOutputScript(output.script);
                      addresses.push(outputAddress);
                    } catch (_e) {
                    }
                  }
                  return addresses;
                };
                replacedTransaction = Transaction.fromHex((transaction == null ? void 0 : transaction.hex) || txHex);
                retrying = true;
                const block = await withRetry(() => getAction(client, getBlock, "getBlock")({
                  blockNumber
                }), {
                  delay: retryDelay,
                  retryCount
                  // shouldRetry: ({ error }) =>
                  //   error instanceof BlockNotFoundError,
                });
                retrying = false;
                const replacedTransactionInputs = /* @__PURE__ */ new Set();
                for (const input of replacedTransaction.ins) {
                  const txid = Array.from(input.hash).reverse().map((byte) => `00${byte.toString(16)}`.slice(-2)).join("");
                  const vout = input.index;
                  const inputId = `${txid}:${vout}`;
                  replacedTransactionInputs.add(inputId);
                }
                let replacementTransaction;
                for (const tx of block.transactions) {
                  if (tx.isCoinbase()) {
                    continue;
                  }
                  for (const input of tx.ins) {
                    const txid = Array.from(input.hash).reverse().map((byte) => `00${byte.toString(16)}`.slice(-2)).join("");
                    const vout = input.index;
                    const inputId = `${txid}:${vout}`;
                    if (replacedTransactionInputs.has(inputId)) {
                      replacementTransaction = tx;
                      break;
                    }
                  }
                  if (replacementTransaction) {
                    break;
                  }
                }
                if (!replacementTransaction) {
                  return;
                }
                transaction = await getAction(client, getUTXOTransaction, "getUTXOTransaction")({
                  txId: replacementTransaction.getId()
                });
                if (transaction.confirmations && transaction.confirmations < confirmations) {
                  return;
                }
                let reason = "replaced";
                const originalOutputAddresses = getOutputAddresses(replacedTransaction);
                const replacementOutputAddresses = getOutputAddresses(replacementTransaction);
                if (originalOutputAddresses.length === replacementOutputAddresses.length && originalOutputAddresses.every((address) => replacementOutputAddresses.includes(address))) {
                  reason = "repriced";
                } else if (senderAddress && replacementOutputAddresses.length === 1 && replacementOutputAddresses.includes(senderAddress)) {
                  reason = "cancelled";
                }
                done(() => {
                  var _a;
                  (_a = emit.onReplaced) == null ? void 0 : _a.call(emit, {
                    reason,
                    replacedTransaction,
                    transaction
                  });
                  emit.resolve(transaction);
                });
              } catch (err_) {
                done(() => emit.reject(err_));
              }
            } else {
              done(() => emit.reject(err));
            }
          } finally {
            count++;
          }
        }
      });
    });
  });
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/actions/getUTXOs.js
var import_dist61 = __toESM(require_dist(), 1);
var import_dist62 = __toESM(require_dist2(), 1);
var import_dist63 = __toESM(require_dist3(), 1);
async function getUTXOs(client, { address, minValue }) {
  const data = await client.request({
    method: "getUTXOs",
    params: {
      address,
      minValue
    }
  });
  return data;
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/actions/getTransactions.js
var import_dist64 = __toESM(require_dist(), 1);
var import_dist65 = __toESM(require_dist2(), 1);
var import_dist66 = __toESM(require_dist3(), 1);
async function getTransactions(client, { address, offset, limit, lastBlock }) {
  const data = await client.request({
    method: "getTransactions",
    params: {
      address,
      offset,
      limit,
      lastBlock
    }
  });
  return data;
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/clients/decorators/public.js
var import_dist67 = __toESM(require_dist(), 1);
var import_dist68 = __toESM(require_dist2(), 1);
var import_dist69 = __toESM(require_dist3(), 1);
function publicActions(client) {
  return {
    getBalance: (args) => getBalance(client, args),
    getBlock: (args) => getBlock(client, args),
    getBlockCount: () => getBlockCount(client),
    getBlockStats: (args) => getBlockStats(client, args),
    getTransactions: (args) => getTransactions(client, args),
    getUTXOTransaction: (args) => getUTXOTransaction(client, args),
    getUTXOs: (args) => getUTXOs(client, args)
  };
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/clients/decorators/wallet.js
var import_dist70 = __toESM(require_dist(), 1);
var import_dist71 = __toESM(require_dist2(), 1);
var import_dist72 = __toESM(require_dist3(), 1);
function walletActions(client) {
  return {
    sendUTXOTransaction: (args) => sendUTXOTransaction(client, args),
    signPbst: (args) => signPsbt(client, args)
  };
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/transports/ankr/methods.js
var import_dist82 = __toESM(require_dist(), 1);
var import_dist83 = __toESM(require_dist2(), 1);
var import_dist84 = __toESM(require_dist3(), 1);

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/transports/ankr/getBalance.js
var import_dist73 = __toESM(require_dist(), 1);
var import_dist74 = __toESM(require_dist2(), 1);
var import_dist75 = __toESM(require_dist3(), 1);
var getBalance2 = async (client, { baseUrl }, { address }) => {
  const apiUrl = `${baseUrl}/address/${address}?details=basic`;
  const response = await client.request({
    url: apiUrl,
    fetchOptions: {
      method: "GET"
    }
  });
  if (response.error) {
    return {
      error: { code: -1, message: response.error }
    };
  }
  return {
    result: BigInt(response.balance)
  };
};

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/transports/ankr/getTransactions.js
var import_dist79 = __toESM(require_dist(), 1);
var import_dist80 = __toESM(require_dist2(), 1);
var import_dist81 = __toESM(require_dist3(), 1);

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/utils/url.js
var import_dist76 = __toESM(require_dist(), 1);
var import_dist77 = __toESM(require_dist2(), 1);
var import_dist78 = __toESM(require_dist3(), 1);
var urlWithParams = (url, params) => {
  const _url = new URL(url);
  Object.entries(params).forEach(([key, value]) => {
    if (value !== void 0) {
      _url.searchParams.set(key, String(value));
    }
  });
  return decodeURIComponent(_url.toString());
};

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/transports/ankr/getTransactions.js
var ankrTransactionTransformer = (txn) => {
  var _a, _b;
  return {
    blockhash: txn == null ? void 0 : txn.blockHash,
    blocktime: txn.blockTime,
    confirmations: txn.confirmations,
    hex: txn.hex,
    locktime: (_a = txn == null ? void 0 : txn.coinSpecificData) == null ? void 0 : _a.locktime,
    hash: (_b = txn.coinSpecificData) == null ? void 0 : _b.hash,
    size: txn.size,
    txid: txn.txid,
    version: txn.version,
    vin: txn.vin.map((vin) => ({
      scriptSig: vin.scriptSig,
      sequence: vin.sequence,
      txid: vin.txid,
      vout: vin.vout,
      txinwitness: vin.addresses
    })),
    vsize: txn.vsize,
    vout: txn.vout.map((vout) => ({
      n: vout.n,
      scriptPubKey: vout.scriptPubKey,
      value: vout.value
    }))
  };
};
var getTransactions2 = async (client, { baseUrl }, { address, limit = 50, offset = 0, lastBlock }) => {
  const apiUrl = urlWithParams(`${baseUrl}/address/${address}`, {
    details: "txs",
    pageSize: limit,
    to: lastBlock
  });
  const response = await client.request({
    url: apiUrl,
    fetchOptions: { method: "GET" }
  });
  if (response.error) {
    return { error: response.error };
  }
  const total = response.txs || 0;
  const page = Math.floor(offset / limit) + 1;
  const hasMore = total > offset + response.transactions.length;
  const data = {
    transactions: response.transactions.map(ankrTransactionTransformer),
    total,
    page,
    itemsPerPage: limit,
    hasMore
  };
  return { result: data };
};

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/transports/ankr/methods.js
var ankrMethods = {
  getBalance: getBalance2,
  getTransactions: getTransactions2
};

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/transports/blockchair/methods.js
var import_dist100 = __toESM(require_dist(), 1);
var import_dist101 = __toESM(require_dist2(), 1);
var import_dist102 = __toESM(require_dist3(), 1);

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/transports/blockchair/getBalance.js
var import_dist85 = __toESM(require_dist(), 1);
var import_dist86 = __toESM(require_dist2(), 1);
var import_dist87 = __toESM(require_dist3(), 1);
var getBalance3 = async (client, { baseUrl, apiKey }, { address }) => {
  var _a, _b, _c;
  const apiUrl = urlWithParams(`${baseUrl}/addresses/balances`, {
    key: apiKey,
    addresses: address
  });
  const response = await client.request({
    url: apiUrl,
    fetchOptions: { method: "GET" }
  });
  if (((_a = response.context) == null ? void 0 : _a.code) !== 200) {
    return {
      error: {
        code: (_b = response.context) == null ? void 0 : _b.code,
        message: (_c = response.context) == null ? void 0 : _c.error
      }
    };
  }
  return {
    result: BigInt(response.data[address])
  };
};

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/transports/blockchair/getUTXOs.js
var import_dist97 = __toESM(require_dist(), 1);
var import_dist98 = __toESM(require_dist2(), 1);
var import_dist99 = __toESM(require_dist3(), 1);

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/errors/request.js
var import_dist91 = __toESM(require_dist(), 1);
var import_dist92 = __toESM(require_dist2(), 1);
var import_dist93 = __toESM(require_dist3(), 1);

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/errors/utils.js
var import_dist88 = __toESM(require_dist(), 1);
var import_dist89 = __toESM(require_dist2(), 1);
var import_dist90 = __toESM(require_dist3(), 1);
var getUrl = (url) => url;

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/errors/request.js
var HttpRequestError = class extends BaseError {
  constructor({ body, cause, details, headers, status, url }) {
    super("HTTP request failed.", {
      cause,
      details,
      metaMessages: [
        status && `Status: ${status}`,
        `URL: ${getUrl(url)}`,
        body && `Request body: ${stringify(body)}`
      ].filter(Boolean),
      name: "HttpRequestError"
    });
    this.body = body;
    this.headers = headers;
    this.status = status;
    this.url = url;
  }
};
var RpcRequestError = class extends BaseError {
  constructor({ body, error, url }) {
    super("RPC Request failed.", {
      cause: error,
      details: error.message,
      metaMessages: [`URL: ${getUrl(url)}`, `Request body: ${stringify(body)}`],
      name: "RpcRequestError"
    });
    this.code = error.code;
    this.data = error.data;
  }
};
var SocketClosedError = class extends BaseError {
  constructor({ url } = {}) {
    super("The socket has been closed.", {
      metaMessages: [url && `URL: ${getUrl(url)}`].filter(Boolean),
      name: "SocketClosedError"
    });
  }
};
var TimeoutError = class extends BaseError {
  constructor({ body, url }) {
    super("The request took too long to respond.", {
      details: "The request timed out.",
      metaMessages: [`URL: ${getUrl(url)}`, `Request body: ${stringify(body)}`],
      name: "TimeoutError"
    });
  }
};

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/errors/utxo.js
var import_dist94 = __toESM(require_dist(), 1);
var import_dist95 = __toESM(require_dist2(), 1);
var import_dist96 = __toESM(require_dist3(), 1);
var InsufficientUTXOBalanceError = class extends BaseError {
  constructor({ minValue, address, balance }) {
    super(`Not enough UTXOs required for ${minValue}`, {
      metaMessages: [
        `${address && balance && `Address ${address} has a balance of ${balance}.`}`
      ],
      name: "InsufficientUTXOBalanceError"
    });
  }
};

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/transports/blockchair/getUTXOs.js
var blockChairUTXOTransformer = (scriptHex) => (data) => ({
  blockHeight: data.block_id,
  scriptHex,
  txId: data.transaction_hash,
  value: data.value,
  vout: data.index
});
var MAX_API_LIMIT = "100,100";
var getUTXOs2 = async (client, { baseUrl, apiKey }, { address, minValue }) => {
  async function* fetchUTXOs() {
    var _a, _b;
    let offset = 0;
    let hasMore = true;
    while (hasMore) {
      const apiUrl = urlWithParams(`${baseUrl}/dashboards/addresses/${address}`, {
        key: apiKey,
        offset: `0,${offset}`,
        limit: MAX_API_LIMIT
      });
      const response = await client.request({
        url: apiUrl,
        fetchOptions: { method: "GET" }
      });
      if (((_a = response.context) == null ? void 0 : _a.code) !== 200 && ((_b = response.context) == null ? void 0 : _b.code) !== 404) {
        throw new RpcRequestError({
          url: apiUrl,
          body: {
            method: "fetchUTXOs",
            params: {
              address,
              minValue
            }
          },
          error: {
            code: response.context.code,
            message: response.context.error || "Error fetching utxos"
          }
        });
      }
      if (!response.data || response.data.utxo.length === 0) {
        hasMore = false;
        continue;
      }
      const { limit } = response.context;
      const totalRows = response.data.addresses[address].unspent_output_count;
      if (limit && totalRows) {
        const [, utxoLimit] = String(limit).split(",").map((val) => Number(val));
        hasMore = offset + utxoLimit < totalRows;
        offset += utxoLimit;
      } else {
        hasMore = false;
        offset += 0;
      }
      yield response.data;
    }
  }
  if (minValue) {
    const { result: balance } = await getBalance3(client, { baseUrl, apiKey }, { address });
    if (minValue > Number(balance)) {
      throw new InsufficientUTXOBalanceError({
        minValue,
        address,
        balance: Number(balance)
      });
    }
  }
  const utxos = [];
  let valueCount = 0;
  for await (const batch of fetchUTXOs()) {
    const addressScriptHex = batch.addresses[address].script_hex;
    const utxoBatch = batch.utxo.map(blockChairUTXOTransformer(addressScriptHex));
    utxos.push(...utxoBatch);
    if (minValue) {
      valueCount += utxoBatch.reduce((sum, utxo2) => sum + utxo2.value, 0);
      if (valueCount >= minValue) {
        break;
      }
    }
  }
  return { result: utxos };
};

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/transports/blockchair/methods.js
var blockchairMethods = {
  getBalance: getBalance3,
  getUTXOs: getUTXOs2
};

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/transports/blockcypher/methods.js
var import_dist109 = __toESM(require_dist(), 1);
var import_dist110 = __toESM(require_dist2(), 1);
var import_dist111 = __toESM(require_dist3(), 1);

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/transports/blockcypher/getBalance.js
var import_dist103 = __toESM(require_dist(), 1);
var import_dist104 = __toESM(require_dist2(), 1);
var import_dist105 = __toESM(require_dist3(), 1);
var getBalance4 = async (client, { baseUrl, apiKey }, { address }) => {
  const apiUrl = urlWithParams(`${baseUrl}/addrs/${address}`, {
    token: apiKey
  });
  const response = await client.request({
    url: apiUrl,
    fetchOptions: { method: "GET" }
  });
  if (response.error) {
    return {
      error: { code: -1, message: response.error }
    };
  }
  return {
    result: BigInt(response.balance)
  };
};

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/transports/blockcypher/getUTXOs.js
var import_dist106 = __toESM(require_dist(), 1);
var import_dist107 = __toESM(require_dist2(), 1);
var import_dist108 = __toESM(require_dist3(), 1);
var blockcypherUTXOTransformer = (utxo2) => ({
  blockHeight: utxo2.block_height,
  isConfirmed: Boolean(utxo2.confirmations),
  confirmations: utxo2.confirmations,
  value: utxo2.value,
  vout: utxo2.tx_output_n,
  txId: utxo2.tx_hash,
  scriptHex: utxo2.script
});
var MAX_API_LIMIT2 = 2e3;
var getUTXOs3 = async (client, { baseUrl, apiKey }, { address, minValue }) => {
  async function* fetchUTXOs() {
    let hasMore = true;
    let beforeBlock = void 0;
    while (hasMore) {
      const apiUrl = urlWithParams(`${baseUrl}/addrs/${address}`, {
        token: apiKey,
        unspentOnly: "true",
        includeScript: "true",
        before: beforeBlock,
        limit: MAX_API_LIMIT2
      });
      const response = await client.request({
        url: apiUrl,
        fetchOptions: { method: "GET" }
      });
      if (response.error) {
        throw new RpcRequestError({
          url: apiUrl,
          body: {
            method: "fetchUTXOs",
            params: {
              address,
              minValue
            }
          },
          error: {
            code: -1,
            message: response.error
          }
        });
      }
      if (minValue && minValue > response.final_balance) {
        throw new InsufficientUTXOBalanceError({
          minValue,
          address,
          balance: response.final_balance
        });
      }
      if (!response.txrefs || response.txrefs.length === 0) {
        hasMore = false;
        continue;
      }
      const { hasMore: apiHasMore } = response;
      hasMore = Boolean(apiHasMore);
      beforeBlock = response.txrefs[response.txrefs.length - 1].block_height;
      yield response.txrefs;
    }
  }
  const utxos = [];
  let valueCount = 0;
  for await (const batch of fetchUTXOs()) {
    const utxoBatch = batch.map(blockcypherUTXOTransformer);
    utxos.push(...utxoBatch);
    if (minValue) {
      valueCount += utxoBatch.reduce((sum, utxo2) => sum + utxo2.value, 0);
      if (valueCount >= minValue) {
        break;
      }
    }
  }
  return { result: utxos };
};

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/transports/blockcypher/methods.js
var blockcypherMethods = {
  getBalance: getBalance4,
  getUTXOs: getUTXOs3
};

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/transports/getHttpRpcClient.js
var import_dist115 = __toESM(require_dist(), 1);
var import_dist116 = __toESM(require_dist2(), 1);
var import_dist117 = __toESM(require_dist3(), 1);

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/utils/withTimeout.js
var import_dist112 = __toESM(require_dist(), 1);
var import_dist113 = __toESM(require_dist2(), 1);
var import_dist114 = __toESM(require_dist3(), 1);
function withTimeout(fn, { errorInstance = new Error("timed out"), timeout, signal }) {
  return new Promise((resolve, reject) => {
    ;
    (async () => {
      let timeoutId;
      try {
        const controller = new AbortController();
        if (timeout > 0) {
          timeoutId = setTimeout(() => {
            if (signal) {
              controller.abort();
            } else {
              reject(errorInstance);
            }
          }, timeout);
        }
        resolve(await fn({ signal: (controller == null ? void 0 : controller.signal) || null }));
      } catch (err) {
        if ((err == null ? void 0 : err.name) === "AbortError") {
          reject(errorInstance);
        }
        reject(err);
      } finally {
        clearTimeout(timeoutId);
      }
    })();
  });
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/transports/getHttpRpcClient.js
function getHttpRpcClient(url, options = {}) {
  return {
    async request(params) {
      var _a;
      const { body, onRequest = options.onRequest, onResponse = options.onResponse, timeout = options.timeout ?? 1e4 } = params;
      const fetchOptions = {
        ...options.fetchOptions ?? {},
        ...params.fetchOptions ?? {}
      };
      const { headers, method, signal: signal_ } = fetchOptions;
      try {
        const response = await withTimeout(async ({ signal }) => {
          const init = {
            ...fetchOptions,
            body: body ? stringify(body) : void 0,
            headers: {
              ...method !== "GET" ? { "Content-Type": "application/json" } : void 0,
              ...headers
            },
            method: method || "POST",
            signal: signal_ || (timeout > 0 ? signal : null)
          };
          const request = new Request(params.url ?? url, init);
          if (onRequest) {
            await onRequest(request, init);
          }
          const response2 = await fetch(params.url ?? url, init);
          return response2;
        }, {
          errorInstance: new TimeoutError({ body: body ?? {}, url }),
          timeout,
          signal: true
        });
        if (onResponse) {
          await onResponse(response);
        }
        let data;
        if ((_a = response.headers.get("Content-Type")) == null ? void 0 : _a.startsWith("application/json")) {
          data = await response.json();
        } else {
          data = await response.text();
          data = JSON.parse(data || "{}");
        }
        if (!response.ok) {
          throw new HttpRequestError({
            body,
            details: stringify(data.error) || response.statusText,
            headers: response.headers,
            status: response.status,
            url
          });
        }
        return data;
      } catch (err) {
        if (err instanceof HttpRequestError) {
          throw err;
        }
        if (err instanceof TimeoutError) {
          throw err;
        }
        throw new HttpRequestError({
          body,
          cause: err,
          url
        });
      }
    }
  };
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/transports/getRpcProviderMethods.js
var import_dist127 = __toESM(require_dist(), 1);
var import_dist128 = __toESM(require_dist2(), 1);
var import_dist129 = __toESM(require_dist3(), 1);

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/transports/mempool/methods.js
var import_dist124 = __toESM(require_dist(), 1);
var import_dist125 = __toESM(require_dist2(), 1);
var import_dist126 = __toESM(require_dist3(), 1);

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/transports/mempool/getBalance.js
var import_dist118 = __toESM(require_dist(), 1);
var import_dist119 = __toESM(require_dist2(), 1);
var import_dist120 = __toESM(require_dist3(), 1);
var getBalance5 = async (client, { baseUrl }, { address }) => {
  const apiUrl = `${baseUrl}/address/${address}`;
  const response = await client.request({
    url: apiUrl,
    fetchOptions: { method: "GET" }
  });
  const balance = response.chain_stats.funded_txo_sum - response.chain_stats.spent_txo_sum;
  return {
    result: BigInt(balance)
  };
};

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/transports/mempool/getTransactions.js
var import_dist121 = __toESM(require_dist(), 1);
var import_dist122 = __toESM(require_dist2(), 1);
var import_dist123 = __toESM(require_dist3(), 1);
var mempoolTransactionTransformer = (txn) => ({
  hash: txn.txid,
  txid: txn.txid,
  vout: txn.vout.map((vout, index2) => ({
    n: index2,
    scriptPubKey: {
      address: vout.scriptpubkey_address,
      asm: vout.scriptpubkey_asm,
      type: vout.scriptpubkey_type,
      desc: vout.scriptpubkey,
      hex: vout.scriptpubkey
    },
    value: vout.value
  })),
  vin: txn.vin.map((vin) => ({
    scriptSig: {
      asm: vin.scriptsig_asm,
      hex: vin.scriptsig
    },
    sequence: vin.sequence,
    txinwitness: vin.witness,
    txid: vin.txid,
    vout: vin.vout
  }))
});
var getTransactions3 = async (client, { baseUrl }, { address, limit = 50, offset = 0, afterTxId }) => {
  const apiUrlAddress = `${baseUrl}/address/${address}`;
  const balanceResponse = await client.request({
    url: apiUrlAddress,
    fetchOptions: { method: "GET" }
  });
  const totalTxns = balanceResponse.chain_stats.tx_count + balanceResponse.mempool_stats.tx_count;
  const apiUrl = urlWithParams(`${baseUrl}/address/${address}/txs`, {
    after_txid: afterTxId
  });
  const response = await client.request({
    url: apiUrl,
    fetchOptions: { method: "GET" }
  });
  const transactions = response.map(mempoolTransactionTransformer);
  const result = {
    transactions,
    total: totalTxns,
    itemsPerPage: limit,
    hasMore: offset + transactions.length < totalTxns
  };
  return {
    result
  };
};

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/transports/mempool/methods.js
var mempoolMethods = {
  getBalance: getBalance5,
  getTransactions: getTransactions3
};

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/transports/getRpcProviderMethods.js
function getRpcProviderMethods(key) {
  switch (key) {
    case "blockchair":
      return blockchairMethods;
    case "ankr":
      return ankrMethods;
    case "blockcypher":
      return blockcypherMethods;
    case "mempool":
      return mempoolMethods;
    default:
      return null;
  }
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/transports/utxo.js
var import_dist154 = __toESM(require_dist(), 1);
var import_dist155 = __toESM(require_dist2(), 1);
var import_dist156 = __toESM(require_dist3(), 1);

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/errors/transport.js
var import_dist130 = __toESM(require_dist(), 1);
var import_dist131 = __toESM(require_dist2(), 1);
var import_dist132 = __toESM(require_dist3(), 1);
var UrlRequiredError = class extends BaseError {
  constructor() {
    super("No URL was provided to the Transport. Please provide a valid RPC URL to the Transport.", {
      name: "UrlRequiredError"
    });
  }
};
var TransportMethodNotSupportedError = class extends BaseError {
  constructor({ method }) {
    super(`No transport found that supports the method: ${method}`, {
      name: "TransportMethodNotSupportedError"
    });
  }
};

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/factories/createTransport.js
var import_dist151 = __toESM(require_dist(), 1);
var import_dist152 = __toESM(require_dist2(), 1);
var import_dist153 = __toESM(require_dist3(), 1);

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/utils/buildRequest.js
var import_dist145 = __toESM(require_dist(), 1);
var import_dist146 = __toESM(require_dist2(), 1);
var import_dist147 = __toESM(require_dist3(), 1);

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/errors/rpc.js
var import_dist133 = __toESM(require_dist(), 1);
var import_dist134 = __toESM(require_dist2(), 1);
var import_dist135 = __toESM(require_dist3(), 1);
var RpcErrorCode;
(function(RpcErrorCode2) {
  RpcErrorCode2[RpcErrorCode2["PARSE_ERROR"] = -32700] = "PARSE_ERROR";
  RpcErrorCode2[RpcErrorCode2["INVALID_REQUEST"] = -32600] = "INVALID_REQUEST";
  RpcErrorCode2[RpcErrorCode2["METHOD_NOT_FOUND"] = -32601] = "METHOD_NOT_FOUND";
  RpcErrorCode2[RpcErrorCode2["INVALID_PARAMS"] = -32602] = "INVALID_PARAMS";
  RpcErrorCode2[RpcErrorCode2["INTERNAL_ERROR"] = -32603] = "INTERNAL_ERROR";
  RpcErrorCode2[RpcErrorCode2["USER_REJECTION"] = -32e3] = "USER_REJECTION";
  RpcErrorCode2[RpcErrorCode2["METHOD_NOT_SUPPORTED"] = -32001] = "METHOD_NOT_SUPPORTED";
  RpcErrorCode2[RpcErrorCode2["ACCESS_DENIED"] = -32002] = "ACCESS_DENIED";
})(RpcErrorCode || (RpcErrorCode = {}));
var UserRejectedRequestError = class extends BaseError {
  constructor(message) {
    const name = "UserRejectedRequestError";
    const errMessage = message || "The user rejected your request, please try again";
    super(`${name}:  ${errMessage}`, {
      name
    });
    this.code = RpcErrorCode.USER_REJECTION;
  }
};
var MethodNotSupportedRpcError = class extends BaseError {
  constructor(method) {
    const name = "MethodNotSupportedError";
    const message = `The method ${method} you are calling is not supported`;
    super(`${name}: ${message}`, {
      name
    });
    this.code = RpcErrorCode.METHOD_NOT_SUPPORTED;
  }
};
var ParseError = class extends BaseError {
  constructor(message) {
    const name = "ParseError";
    const errMessage = message || "Invalid JSON, there was an error parsing your request.";
    super(`${name}: ${errMessage}`, {
      name
    });
    this.code = RpcErrorCode.PARSE_ERROR;
  }
};

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/utils/converter.js
var import_dist136 = __toESM(require_dist(), 1);
var import_dist137 = __toESM(require_dist2(), 1);
var import_dist138 = __toESM(require_dist3(), 1);
function hexToBase64(hex) {
  const raw = hex.match(/.{1,2}/g).map((byte) => String.fromCharCode(Number.parseInt(byte, 16))).join("");
  return btoa(raw);
}
function base64ToHex(base64) {
  const raw = atob(base64);
  let hex = "";
  for (let i = 0; i < raw.length; i++) {
    const hexByte = raw.charCodeAt(i).toString(16);
    hex += hexByte.length === 2 ? hexByte : `0${hexByte}`;
  }
  return hex;
}
function base64urlEncode(str) {
  const bytes = new TextEncoder().encode(str);
  const encodedString = btoa(String.fromCharCode(...bytes));
  return encodedString.replace(/\+/g, "-").replace(/\//g, "_").replace(/=+$/, "");
}
function stringToHex(value) {
  const hex = Array.from(value).map((char) => char.charCodeAt(0).toString(16).padStart(2, "0")).join("");
  return `0x${hex}`;
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/utils/withDedupe.js
var import_dist142 = __toESM(require_dist(), 1);
var import_dist143 = __toESM(require_dist2(), 1);
var import_dist144 = __toESM(require_dist3(), 1);

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/utils/lru.js
var import_dist139 = __toESM(require_dist(), 1);
var import_dist140 = __toESM(require_dist2(), 1);
var import_dist141 = __toESM(require_dist3(), 1);
var LruMap = class extends Map {
  constructor(size2) {
    super();
    this.maxSize = size2;
  }
  get(key) {
    const value = super.get(key);
    if (super.has(key) && value !== void 0) {
      this.delete(key);
      super.set(key, value);
    }
    return value;
  }
  set(key, value) {
    super.set(key, value);
    if (this.maxSize && this.size > this.maxSize) {
      const firstKey = this.keys().next().value;
      if (firstKey) {
        this.delete(firstKey);
      }
    }
    return this;
  }
};

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/utils/withDedupe.js
var promiseCache = new LruMap(8192);
function withDedupe(fn, { enabled = true, id }) {
  if (!enabled || !id) {
    return fn();
  }
  if (promiseCache.get(id)) {
    return promiseCache.get(id);
  }
  const promise = fn().finally(() => promiseCache.delete(id));
  promiseCache.set(id, promise);
  return promise;
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/utils/buildRequest.js
function buildRequest(request, options = {}) {
  return async (args, overrideOptions = {}) => {
    var _a;
    const { dedupe = false, methods, retryDelay = 150, retryCount = 3, uid: uid2 } = {
      ...options,
      ...overrideOptions
    };
    const { method } = args;
    if ((_a = methods == null ? void 0 : methods.exclude) == null ? void 0 : _a.includes(method)) {
      throw new MethodNotSupportedRpcError(method);
    }
    if ((methods == null ? void 0 : methods.include) && !methods.include.includes(method)) {
      throw new MethodNotSupportedRpcError(method);
    }
    const requestId = dedupe ? stringToHex(`${uid2}.${stringify(args)}`) : void 0;
    return withDedupe(() => withRetry(async () => {
      try {
        return await request(args);
      } catch (err_) {
        const err = err_;
        switch (err.code) {
          case RpcErrorCode.METHOD_NOT_SUPPORTED:
            throw new MethodNotSupportedRpcError(method);
          case RpcErrorCode.USER_REJECTION:
            throw new UserRejectedRequestError(err.message);
          case 5e3:
            throw new UserRejectedRequestError(err.message);
          default:
            if (err_ instanceof BaseError) {
              throw err_;
            }
            throw new BaseError("Unknown Error", { cause: err_ });
        }
      }
    }, {
      delay: ({ count, error }) => {
        var _a2;
        if (error && error instanceof HttpRequestError) {
          const retryAfter = (_a2 = error == null ? void 0 : error.headers) == null ? void 0 : _a2.get("Retry-After");
          if (retryAfter == null ? void 0 : retryAfter.match(/\d/)) {
            return Number.parseInt(retryAfter) * 1e3;
          }
        }
        return ~~(1 << count) * retryDelay;
      },
      retryCount,
      shouldRetry: ({ error }) => shouldRetry(error)
    }), { enabled: dedupe, id: requestId });
  };
}
function shouldRetry(error) {
  if ("code" in error && typeof error.code === "number") {
    if (error.code === -1) {
      return true;
    }
    if (error.code === RpcErrorCode.INTERNAL_ERROR) {
      return true;
    }
    return false;
  }
  if (error instanceof HttpRequestError && error.status) {
    if (error.status === 403) {
      return true;
    }
    if (error.status === 408) {
      return true;
    }
    if (error.status === 413) {
      return true;
    }
    if (error.status === 429) {
      return true;
    }
    if (error.status === 500) {
      return true;
    }
    if (error.status === 502) {
      return true;
    }
    if (error.status === 503) {
      return true;
    }
    if (error.status === 504) {
      return true;
    }
    return false;
  }
  return true;
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/utils/uid.js
var import_dist148 = __toESM(require_dist(), 1);
var import_dist149 = __toESM(require_dist2(), 1);
var import_dist150 = __toESM(require_dist3(), 1);
var size = 256;
var index = size;
var buffer;
function uid(length = 11) {
  if (!buffer || index + length > size * 2) {
    buffer = "";
    index = 0;
    for (let i = 0; i < size; i++) {
      buffer += (256 + Math.random() * 256 | 0).toString(16).substring(1);
    }
  }
  return buffer.substring(index, index++ + length);
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/factories/createTransport.js
function createTransport({ key, methods, name, request, retryCount = 3, retryDelay = 150, timeout, type }, value) {
  const uid2 = uid();
  return {
    config: {
      key,
      methods,
      name,
      request,
      retryCount,
      retryDelay,
      timeout,
      type
    },
    request: buildRequest(request, { methods, retryCount, retryDelay, uid: uid2 }),
    value
  };
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/transports/utxo.js
function utxo(url, config = {}) {
  const { fetchOptions, key = "utxo", name = "UTXO HTTP API", onFetchRequest, onFetchResponse, retryDelay, apiKey, methods } = config;
  return ({ chain, retryCount: retryCount_, timeout: timeout_ }) => {
    const retryCount = config.retryCount ?? retryCount_;
    const timeout = timeout_ ?? config.timeout ?? 1e4;
    let url_ = url;
    if (config.includeChainToURL) {
      const chainName = chain == null ? void 0 : chain.name.replace(" ", "-").toLowerCase();
      url_ = `${url}${chainName ? `/${chainName}` : ""}`;
    }
    if (!url_) {
      throw new UrlRequiredError();
    }
    const client = getHttpRpcClient(url_, {
      fetchOptions,
      onRequest: onFetchRequest,
      onResponse: onFetchResponse,
      timeout
    });
    const rpcMethods = getRpcProviderMethods(key);
    return createTransport({
      key,
      name,
      methods,
      async request({ method, params }) {
        const body = { method, params };
        const methodHandler = rpcMethods == null ? void 0 : rpcMethods[method];
        const { error, result } = await ((methodHandler == null ? void 0 : methodHandler(client, { baseUrl: url_, apiKey }, params)) ?? client.request({
          body
        }));
        if (error) {
          throw new RpcRequestError({
            body,
            error,
            url: url_
          });
        }
        return result;
      },
      retryCount,
      retryDelay,
      timeout,
      type: "http"
    }, {
      fetchOptions,
      url: url_
    });
  };
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/transports/custom.js
var import_dist157 = __toESM(require_dist(), 1);
var import_dist158 = __toESM(require_dist2(), 1);
var import_dist159 = __toESM(require_dist3(), 1);
function custom(provider, config = {}) {
  const { key = "custom", methods, name = "Custom Provider", retryDelay } = config;
  return ({ retryCount: defaultRetryCount }) => createTransport({
    key,
    methods,
    name,
    request: provider.request.bind(provider),
    retryCount: config.retryCount ?? defaultRetryCount,
    retryDelay,
    type: "custom"
  });
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/transports/ankr/ankr.js
var import_dist160 = __toESM(require_dist(), 1);
var import_dist161 = __toESM(require_dist2(), 1);
var import_dist162 = __toESM(require_dist3(), 1);
var ankr = (config) => {
  const methods = {
    include: Object.keys(ankrMethods)
  };
  if (config == null ? void 0 : config.baseUrl) {
    return utxo(config.baseUrl, { key: "ankr", ...config, methods });
  }
  if (!(config == null ? void 0 : config.apiKey)) {
    throw Error("Ankr API KEY is required");
  }
  return utxo(`https://rpc.ankr.com/premium-http/btc_blockbook/${config.apiKey}/api/v2`, {
    key: "ankr",
    methods,
    ...config
  });
};

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/transports/blockchair/blockchair.js
var import_dist163 = __toESM(require_dist(), 1);
var import_dist164 = __toESM(require_dist2(), 1);
var import_dist165 = __toESM(require_dist3(), 1);
var blockchair = (config) => utxo((config == null ? void 0 : config.baseUrl) || "https://api.blockchair.com", {
  key: "blockchair",
  includeChainToURL: true,
  methods: {
    include: Object.keys(blockchairMethods)
  },
  ...config
});

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/transports/blockcypher/blockcypher.js
var import_dist166 = __toESM(require_dist(), 1);
var import_dist167 = __toESM(require_dist2(), 1);
var import_dist168 = __toESM(require_dist3(), 1);
var blockcypher = (config) => utxo((config == null ? void 0 : config.baseUrl) || "https://api.blockcypher.com/v1/btc/main", {
  key: "blockcypher",
  methods: {
    include: Object.keys(blockcypherMethods)
  },
  ...config
});

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/transports/mempool/mempool.js
var import_dist169 = __toESM(require_dist(), 1);
var import_dist170 = __toESM(require_dist2(), 1);
var import_dist171 = __toESM(require_dist3(), 1);
var mempool = (config) => utxo((config == null ? void 0 : config.baseUrl) || "https://mempool.space/api", {
  key: "mempool",
  methods: {
    include: Object.keys(mempoolMethods)
  },
  ...config
});

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/utils/cancelTransaction.js
var import_dist172 = __toESM(require_dist(), 1);
var import_dist173 = __toESM(require_dist2(), 1);
var import_dist174 = __toESM(require_dist3(), 1);
function cancelTransaction(psbt, accountAddress) {
  const newPsbt = new Psbt();
  const inputs = psbt.data.inputs;
  const txInputs = psbt.txInputs;
  for (let i = 0; i < inputs.length; i++) {
    const input = inputs[i];
    const txInput = txInputs[i];
    const inputData = {
      hash: txInput.hash,
      index: txInput.index
    };
    if (input.nonWitnessUtxo) {
      inputData.nonWitnessUtxo = input.nonWitnessUtxo;
    } else if (input.witnessUtxo) {
      inputData.witnessUtxo = input.witnessUtxo;
    } else {
      throw new Error("Input UTXO information is missing");
    }
    if (input.redeemScript) {
      inputData.redeemScript = input.redeemScript;
    }
    if (input.witnessScript) {
      inputData.witnessScript = input.witnessScript;
    }
    newPsbt.addInput(inputData);
  }
  const outputs = psbt.txOutputs;
  let totalOutputValue = BigInt(0);
  for (const output of outputs) {
    totalOutputValue += output.value;
  }
  if (totalOutputValue <= BigInt(0)) {
    throw new Error("Total output value must be greater than zero");
  }
  const outputScript = address_exports.toOutputScript(accountAddress);
  newPsbt.addOutput({
    script: outputScript,
    value: totalOutputValue
  });
  newPsbt.txInputs.forEach((_, index2) => {
    newPsbt.setInputSequence(index2, 4294967293);
  });
  return newPsbt;
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/utils/getUTXOAddress.js
var import_dist175 = __toESM(require_dist(), 1);
var import_dist176 = __toESM(require_dist2(), 1);
var import_dist177 = __toESM(require_dist3(), 1);
init_sha256();
var import_bech32 = __toESM(require_dist4(), 1);
var import_bs58 = __toESM(require_bs58(), 1);
var UTXONetwork;
(function(UTXONetwork2) {
  UTXONetwork2["Mainnet"] = "mainnet";
  UTXONetwork2["Testnet"] = "testnet";
  UTXONetwork2["Regtest"] = "regtest";
})(UTXONetwork || (UTXONetwork = {}));
var UTXOAddressType;
(function(UTXOAddressType2) {
  UTXOAddressType2["p2pkh"] = "p2pkh";
  UTXOAddressType2["p2sh"] = "p2sh";
  UTXOAddressType2["p2wpkh"] = "p2wpkh";
  UTXOAddressType2["p2wsh"] = "p2wsh";
  UTXOAddressType2["p2tr"] = "p2tr";
})(UTXOAddressType || (UTXOAddressType = {}));
var addressTypes = {
  0: {
    type: UTXOAddressType.p2pkh,
    network: UTXONetwork.Mainnet
  },
  111: {
    type: UTXOAddressType.p2pkh,
    network: UTXONetwork.Testnet
  },
  5: {
    type: UTXOAddressType.p2sh,
    network: UTXONetwork.Mainnet
  },
  196: {
    type: UTXOAddressType.p2sh,
    network: UTXONetwork.Testnet
  }
};
var parseBech32 = (address) => {
  let decoded;
  try {
    if (address.startsWith("bc1p") || address.startsWith("tb1p") || address.startsWith("bcrt1p")) {
      decoded = import_bech32.bech32m.decode(address);
    } else {
      decoded = import_bech32.bech32.decode(address);
    }
  } catch (_error) {
    throw new Error("Invalid address");
  }
  const mapPrefixToNetwork = {
    bc: UTXONetwork.Mainnet,
    tb: UTXONetwork.Testnet,
    bcrt: UTXONetwork.Regtest
  };
  const network = mapPrefixToNetwork[decoded.prefix];
  if (network === void 0) {
    throw new Error("Invalid address");
  }
  const witnessVersion = decoded.words[0];
  if (witnessVersion < 0 || witnessVersion > 16) {
    throw new Error("Invalid address");
  }
  const data = import_bech32.bech32.fromWords(decoded.words.slice(1));
  let type;
  if (data.length === 20) {
    type = UTXOAddressType.p2wpkh;
  } else if (witnessVersion === 1) {
    type = UTXOAddressType.p2tr;
  } else {
    type = UTXOAddressType.p2wsh;
  }
  return {
    bech32: true,
    network,
    address,
    type
  };
};
var getUTXOAddress = (address) => {
  let decoded;
  const prefix = address.substring(0, 2).toLowerCase();
  if (prefix === "bc" || prefix === "tb") {
    return parseBech32(address);
  }
  try {
    decoded = import_bs58.default.decode(address);
  } catch (_error) {
    throw new Error("Invalid address");
  }
  const { length } = decoded;
  if (length !== 25) {
    throw new Error("Invalid address");
  }
  const version2 = decoded[0];
  const checksum = decoded.slice(length - 4, length);
  const body = decoded.slice(0, length - 4);
  const expectedChecksum = sha256(sha256(body)).slice(0, 4);
  if (checksum.some((value, index2) => value !== expectedChecksum[index2])) {
    throw new Error("Invalid address");
  }
  const validVersions = Object.keys(addressTypes).map(Number);
  if (!validVersions.includes(version2)) {
    throw new Error("Invalid address");
  }
  const addressType = addressTypes[version2];
  return {
    ...addressType,
    address,
    bech32: false
  };
};

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/utils/isUTXOAddress.js
var import_dist178 = __toESM(require_dist(), 1);
var import_dist179 = __toESM(require_dist2(), 1);
var import_dist180 = __toESM(require_dist3(), 1);
var isUTXOAddress = (address, network) => {
  try {
    const utxoAddress = getUTXOAddress(address);
    if (network) {
      return network === utxoAddress.network;
    }
    return true;
  } catch (_error) {
    return false;
  }
};

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/utils/modifyFee.js
var import_dist181 = __toESM(require_dist(), 1);
var import_dist182 = __toESM(require_dist2(), 1);
var import_dist183 = __toESM(require_dist3(), 1);
function modifyFee(psbt, newFee, accountAddress) {
  const newPsbt = new Psbt();
  const inputs = psbt.data.inputs;
  const outputs = psbt.txOutputs;
  for (let i = 0; i < inputs.length; i++) {
    const input = inputs[i];
    const inputData = {
      hash: psbt.txInputs[i].hash,
      index: psbt.txInputs[i].index
    };
    if (input.nonWitnessUtxo) {
      inputData.nonWitnessUtxo = input.nonWitnessUtxo;
    } else if (input.witnessUtxo) {
      inputData.witnessUtxo = input.witnessUtxo;
    }
    if (input.redeemScript) {
      inputData.redeemScript = input.redeemScript;
    }
    if (input.witnessScript) {
      inputData.witnessScript = input.witnessScript;
    }
    newPsbt.addInput(inputData);
  }
  const changeOutputScript = address_exports.toOutputScript(accountAddress);
  for (const output of outputs) {
    const outputData = {
      script: output.script,
      value: output.value
    };
    const scriptsAreEqual = output.script.length === changeOutputScript.length && output.script.every((value, index2) => value === changeOutputScript[index2]);
    if (scriptsAreEqual) {
      outputData.value = output.value - newFee;
      if (outputData.value < 0) {
        throw new Error("Insufficient funds to adjust the fee by the specified amount.");
      }
    }
    newPsbt.addOutput(outputData);
  }
  newPsbt.txInputs.forEach((_, index2) => {
    newPsbt.setInputSequence(index2, 4294967293);
  });
  return newPsbt;
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/utils/parseAccount.js
var import_dist184 = __toESM(require_dist(), 1);
var import_dist185 = __toESM(require_dist2(), 1);
var import_dist186 = __toESM(require_dist3(), 1);
function parseAccount(account) {
  if (typeof account === "string") {
    return { address: account, type: "json-rpc" };
  }
  return account;
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/utils/deepEqual.js
var import_dist187 = __toESM(require_dist(), 1);
var import_dist188 = __toESM(require_dist2(), 1);
var import_dist189 = __toESM(require_dist3(), 1);
function deepEqual(a, b) {
  if (a === b) {
    return true;
  }
  if (a && b && typeof a === "object" && typeof b === "object") {
    if (a.constructor !== b.constructor) {
      return false;
    }
    let length;
    let i;
    if (Array.isArray(a) && Array.isArray(b)) {
      length = a.length;
      if (length !== b.length) {
        return false;
      }
      for (i = length; i-- !== 0; ) {
        if (!deepEqual(a[i], b[i])) {
          return false;
        }
      }
      return true;
    }
    if (a.valueOf !== Object.prototype.valueOf) {
      return a.valueOf() === b.valueOf();
    }
    if (a.toString !== Object.prototype.toString) {
      return a.toString() === b.toString();
    }
    const keys = Object.keys(a);
    length = keys.length;
    if (length !== Object.keys(b).length) {
      return false;
    }
    for (i = length; i-- !== 0; ) {
      if (!Object.prototype.hasOwnProperty.call(b, keys[i])) {
        return false;
      }
    }
    for (i = length; i-- !== 0; ) {
      const key = keys[i];
      if (key && !deepEqual(a[key], b[key])) {
        return false;
      }
    }
    return true;
  }
  return a !== a && b !== b;
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/factories/createClient.js
var import_dist190 = __toESM(require_dist(), 1);
var import_dist191 = __toESM(require_dist2(), 1);
var import_dist192 = __toESM(require_dist3(), 1);
function createClient(parameters) {
  const { cacheTime = parameters.pollingInterval ?? 4e3, key = "base", name = "Base Client", pollingInterval = 4e3, type = "base" } = parameters;
  const chain = parameters.chain;
  const account = parameters.account ? parseAccount(parameters.account) : void 0;
  const { config, request, value } = parameters.transport({
    chain,
    pollingInterval
  });
  const transport = { ...config, ...value };
  const client = {
    account,
    cacheTime,
    chain,
    key,
    name,
    pollingInterval,
    request,
    transport,
    type,
    uid: uid()
  };
  function extend(base) {
    return (extendFn) => {
      const extended = extendFn(base);
      for (const key2 in client) {
        delete extended[key2];
      }
      const combined = { ...base, ...extended };
      return Object.assign(combined, { extend: extend(combined) });
    };
  }
  return Object.assign(client, { extend: extend(client) });
}
function rpcSchema() {
  return null;
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/chains/bitcoin.js
var import_dist196 = __toESM(require_dist(), 1);
var import_dist197 = __toESM(require_dist2(), 1);
var import_dist198 = __toESM(require_dist3(), 1);

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/chains/defineChain.js
var import_dist193 = __toESM(require_dist(), 1);
var import_dist194 = __toESM(require_dist2(), 1);
var import_dist195 = __toESM(require_dist3(), 1);
function defineChain(chain) {
  return {
    formatters: void 0,
    fees: void 0,
    serializers: void 0,
    ...chain
  };
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/chains/bitcoin.js
var bitcoin = defineChain({
  id: 20000000000001,
  name: "Bitcoin",
  nativeCurrency: { name: "Bitcoin", symbol: "BTC", decimals: 8 },
  rpcUrls: {
    default: {
      http: ["https://node-router.thorswap.net/bitcoin"]
    }
  },
  blockExplorers: {
    default: {
      name: "Mempool",
      url: "https://mempool.space/"
    }
  }
});

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/errors/address.js
var import_dist199 = __toESM(require_dist(), 1);
var import_dist200 = __toESM(require_dist2(), 1);
var import_dist201 = __toESM(require_dist3(), 1);
var InvalidAddressError = class extends BaseError {
  constructor({ address }) {
    super(`Address "${address}" is invalid.`, {
      metaMessages: [
        "- Address must be a hex value of 20 bytes (40 hex characters).",
        "- Address must match its checksum counterpart."
      ],
      name: "InvalidAddressError"
    });
  }
};

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/errors/config.js
var import_dist202 = __toESM(require_dist(), 1);
var import_dist203 = __toESM(require_dist2(), 1);
var import_dist204 = __toESM(require_dist3(), 1);
var ChainNotConfiguredError = class extends BaseError {
  constructor() {
    super("Chain not configured.");
    this.name = "ChainNotConfiguredError";
  }
};
var ConnectorAlreadyConnectedError = class extends BaseError {
  constructor() {
    super("Connector already connected.");
    this.name = "ConnectorAlreadyConnectedError";
  }
};
var ConnectorNotConnectedError = class extends BaseError {
  constructor() {
    super("Connector not connected.");
    this.name = "ConnectorNotConnectedError";
  }
};
var ConnectorNotFoundError = class extends BaseError {
  constructor() {
    super("Connector not found.");
    this.name = "ConnectorNotFoundError";
  }
};
var ConnectorAccountNotFoundError = class extends BaseError {
  constructor({ address, connector }) {
    super(`Account "${address}" not found for connector "${connector}".`);
    this.name = "ConnectorAccountNotFoundError";
  }
};
var ConnectorChainMismatchError = class extends BaseError {
  constructor({ connectionChainId, connectorChainId }) {
    super(`The current chain of the connector (id: ${connectorChainId}) does not match the connection's chain (id: ${connectionChainId}).`, {
      metaMessages: [
        `Current Chain ID:  ${connectorChainId}`,
        `Expected Chain ID: ${connectionChainId}`
      ]
    });
    this.name = "ConnectorChainMismatchError";
  }
};

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/errors/provider.js
var import_dist205 = __toESM(require_dist(), 1);
var import_dist206 = __toESM(require_dist2(), 1);
var import_dist207 = __toESM(require_dist3(), 1);
var ProviderNotFoundError = class extends BaseError {
  constructor(message) {
    const name = "ProviderNotFoundError";
    const defaultMessage = "The provider for this connector was not found";
    super(`${name}: ${message || defaultMessage}`, {
      name
    });
  }
};

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/transports/http.js
var import_dist217 = __toESM(require_dist(), 1);
var import_dist218 = __toESM(require_dist2(), 1);
var import_dist219 = __toESM(require_dist3(), 1);

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/utils/createBatchScheduler.js
var import_dist211 = __toESM(require_dist(), 1);
var import_dist212 = __toESM(require_dist2(), 1);
var import_dist213 = __toESM(require_dist3(), 1);

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/utils/withResolvers.js
var import_dist208 = __toESM(require_dist(), 1);
var import_dist209 = __toESM(require_dist2(), 1);
var import_dist210 = __toESM(require_dist3(), 1);
function withResolvers() {
  let resolve = () => void 0;
  let reject = () => void 0;
  const promise = new Promise((resolve_, reject_) => {
    resolve = resolve_;
    reject = reject_;
  });
  return { promise, resolve, reject };
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/utils/createBatchScheduler.js
var schedulerCache = /* @__PURE__ */ new Map();
function createBatchScheduler({ fn, id, shouldSplitBatch, wait: wait2 = 0, sort }) {
  const exec = async () => {
    const scheduler = getScheduler();
    flush();
    const args = scheduler.map(({ args: args2 }) => args2);
    if (args.length === 0) {
      return;
    }
    fn(args).then((data) => {
      if (sort && Array.isArray(data)) {
        data.sort(sort);
      }
      for (let i = 0; i < scheduler.length; i++) {
        const { resolve } = scheduler[i];
        resolve == null ? void 0 : resolve([data[i], data]);
      }
    }).catch((err) => {
      for (let i = 0; i < scheduler.length; i++) {
        const { reject } = scheduler[i];
        reject == null ? void 0 : reject(err);
      }
    });
  };
  const flush = () => schedulerCache.delete(id);
  const getBatchedArgs = () => getScheduler().map(({ args }) => args);
  const getScheduler = () => schedulerCache.get(id) || [];
  const setScheduler = (item) => schedulerCache.set(id, [...getScheduler(), item]);
  return {
    flush,
    async schedule(args) {
      const { promise, resolve, reject } = withResolvers();
      const split = shouldSplitBatch == null ? void 0 : shouldSplitBatch([...getBatchedArgs(), args]);
      if (split) {
        exec();
      }
      const hasActiveScheduler = getScheduler().length > 0;
      if (hasActiveScheduler) {
        setScheduler({ args, resolve, reject });
        return promise;
      }
      setScheduler({ args, resolve, reject });
      setTimeout(exec, wait2);
      return promise;
    }
  };
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/transports/types.js
var import_dist214 = __toESM(require_dist(), 1);
var import_dist215 = __toESM(require_dist2(), 1);
var import_dist216 = __toESM(require_dist3(), 1);
var UTXOAPISchemaMethods = [
  "getBalance",
  "getTransactions",
  "getUTXOs"
];

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/transports/http.js
function http(url, config = {}) {
  const { batch, fetchOptions, key = "http", methods = { exclude: UTXOAPISchemaMethods }, name = "HTTP JSON-RPC", onFetchRequest, onFetchResponse, retryDelay, raw } = config;
  return ({ chain, retryCount: retryCount_, timeout: timeout_ }) => {
    const { batchSize = 1e3, wait: wait2 = 0 } = typeof batch === "object" ? batch : {};
    const retryCount = config.retryCount ?? retryCount_;
    const timeout = timeout_ ?? config.timeout ?? 1e4;
    const url_ = url || (chain == null ? void 0 : chain.rpcUrls.default.http[0]);
    if (!url_) {
      throw new UrlRequiredError();
    }
    const rpcClient = getHttpRpcClient(url_, {
      fetchOptions,
      onRequest: onFetchRequest,
      onResponse: onFetchResponse,
      timeout
    });
    return createTransport({
      key,
      methods,
      name,
      async request({ method, params }) {
        const body = { method, params };
        const { schedule } = createBatchScheduler({
          id: url_,
          wait: wait2,
          shouldSplitBatch(requests) {
            return requests.length > batchSize;
          },
          fn: (body2) => rpcClient.request({
            body: body2
          }),
          sort: (a, b) => a.id - b.id
        });
        const fn = async (body2) => batch ? schedule(body2) : [
          await rpcClient.request({
            body: body2
          })
        ];
        const [{ error, result }] = await fn(body);
        if (raw) {
          return { error, result };
        }
        if (error) {
          throw new RpcRequestError({
            body,
            error,
            url: url_
          });
        }
        return result;
      },
      retryCount,
      retryDelay,
      timeout,
      type: "http"
    }, {
      fetchOptions,
      url: url_
    });
  };
}

// ../../node_modules/.pnpm/@bigmi+core@0.3.1_@types+react@18.3.27_bs58@5.0.0_react@18.3.1_typescript@5.9.3_use-sync-exte_k4ljpnk6mvlf74t5soufxragji/node_modules/@bigmi/core/dist/esm/transports/fallback.js
var import_dist220 = __toESM(require_dist(), 1);
var import_dist221 = __toESM(require_dist2(), 1);
var import_dist222 = __toESM(require_dist3(), 1);
function fallback(transports_, config = {}) {
  const { key = "fallback", name = "Fallback", rank = false, shouldThrow: shouldThrow_ = shouldThrow, retryCount, retryDelay } = config;
  return ({ chain, pollingInterval = 4e3, timeout, ...rest }) => {
    let transports = transports_;
    let onResponse = () => {
    };
    const transport = createTransport({
      key,
      name,
      async request({ method, params }) {
        const supportedTransports = transports.reduce((supportedTransports2, transport2) => {
          const instance = transport2({
            ...rest,
            chain,
            retryCount: 0,
            timeout
          });
          const { include, exclude } = instance.config.methods || {};
          if (include) {
            if (include.includes(method)) {
              supportedTransports2.push(instance);
            }
            return supportedTransports2;
          }
          if (exclude) {
            if (!exclude.includes(method)) {
              supportedTransports2.push(instance);
            }
            return supportedTransports2;
          }
          supportedTransports2.push(instance);
          return supportedTransports2;
        }, []);
        if (!supportedTransports.length) {
          throw new TransportMethodNotSupportedError({ method });
        }
        const fetch2 = async (i = 0) => {
          const transport2 = supportedTransports[i];
          try {
            const response = await transport2.request({
              method,
              params
            });
            onResponse({
              method,
              params,
              response,
              transport: transport2,
              status: "success"
            });
            return response;
          } catch (err) {
            onResponse({
              error: err,
              method,
              params,
              transport: transport2,
              status: "error"
            });
            if (shouldThrow_(err)) {
              throw err;
            }
            if (i === supportedTransports.length - 1) {
              throw err;
            }
            return fetch2(i + 1);
          }
        };
        return fetch2();
      },
      retryCount,
      retryDelay,
      type: "fallback"
    }, {
      onResponse: (fn) => {
        onResponse = fn;
      },
      transports: transports.map((fn) => fn({ chain, retryCount: 0 }))
    });
    if (rank) {
      const rankOptions = typeof rank === "object" ? rank : {};
      rankTransports({
        chain,
        interval: rankOptions.interval ?? pollingInterval,
        onTransports: (transports_2) => {
          transports = transports_2;
        },
        ping: rankOptions.ping,
        sampleCount: rankOptions.sampleCount,
        timeout: rankOptions.timeout,
        transports,
        weights: rankOptions.weights
      });
    }
    return transport;
  };
}
function shouldThrow(error) {
  if ("code" in error && typeof error.code === "number") {
    if (error.code === RpcErrorCode.INTERNAL_ERROR || error.code === RpcErrorCode.USER_REJECTION || error.code === 5e3 || // CAIP UserRejectedRequestError
    error instanceof InsufficientUTXOBalanceError) {
      return true;
    }
  }
  return false;
}
function rankTransports({ chain, interval = 4e3, onTransports, ping, sampleCount = 10, timeout = 1e3, transports, weights = {} }) {
  const { stability: stabilityWeight = 0.7, latency: latencyWeight = 0.3 } = weights;
  const samples = [];
  const rankTransports_ = async () => {
    const sample = await Promise.all(transports.map(async (transport) => {
      const transport_ = transport({ chain, retryCount: 0, timeout });
      const start = Date.now();
      let end;
      let success;
      try {
        await (ping ? ping({ transport: transport_ }) : transport_.request({
          method: "net_listening",
          params: void 0
        }));
        success = 1;
      } catch {
        success = 0;
      } finally {
        end = Date.now();
      }
      const latency = end - start;
      return { latency, success };
    }));
    samples.push(sample);
    if (samples.length > sampleCount) {
      samples.shift();
    }
    const maxLatency = Math.max(...samples.map((sample2) => Math.max(...sample2.map(({ latency }) => latency))));
    const scores = transports.map((_, i) => {
      const latencies = samples.map((sample2) => sample2[i].latency);
      const meanLatency = latencies.reduce((acc, latency) => acc + latency, 0) / latencies.length;
      const latencyScore = 1 - meanLatency / maxLatency;
      const successes = samples.map((sample2) => sample2[i].success);
      const stabilityScore = successes.reduce((acc, success) => acc + success, 0) / successes.length;
      if (stabilityScore === 0) {
        return [0, i];
      }
      return [
        latencyWeight * latencyScore + stabilityWeight * stabilityScore,
        i
      ];
    }).sort((a, b) => b[0] - a[0]);
    onTransports(scores.map(([, i]) => transports[i]));
    await wait(interval);
    rankTransports_();
  };
  rankTransports_();
}

export {
  getBalance,
  version,
  BaseError,
  BlockNotFoundError,
  getBlock,
  getBlockCount,
  getBlockStats,
  TransactionNotFoundError,
  TransactionReceiptNotFoundError,
  WaitForTransactionReceiptTimeoutError,
  getUTXOTransaction,
  sendUTXOTransaction,
  signPsbt,
  listenersCache,
  cleanupCache,
  observe,
  withRetry,
  poll,
  watchBlockNumber,
  waitForTransaction,
  getUTXOs,
  getTransactions,
  publicActions,
  walletActions,
  ankrMethods,
  HttpRequestError,
  RpcRequestError,
  SocketClosedError,
  TimeoutError,
  InsufficientUTXOBalanceError,
  blockchairMethods,
  blockcypherMethods,
  withTimeout,
  getHttpRpcClient,
  mempoolMethods,
  getRpcProviderMethods,
  UrlRequiredError,
  RpcErrorCode,
  UserRejectedRequestError,
  MethodNotSupportedRpcError,
  ParseError,
  hexToBase64,
  base64ToHex,
  base64urlEncode,
  stringToHex,
  uid,
  utxo,
  custom,
  ankr,
  blockchair,
  blockcypher,
  mempool,
  cancelTransaction,
  UTXONetwork,
  UTXOAddressType,
  getUTXOAddress,
  isUTXOAddress,
  modifyFee,
  parseAccount,
  deepEqual,
  createClient,
  rpcSchema,
  defineChain,
  bitcoin,
  InvalidAddressError,
  ChainNotConfiguredError,
  ConnectorAlreadyConnectedError,
  ConnectorNotConnectedError,
  ConnectorNotFoundError,
  ConnectorAccountNotFoundError,
  ConnectorChainMismatchError,
  ProviderNotFoundError,
  http,
  fallback
};
//# sourceMappingURL=chunk-IM5MOWKZ.js.map
